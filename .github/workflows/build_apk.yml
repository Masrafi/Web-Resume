#name: Build and Upload APKs
#
#on:
#  push:
#    branches: [main]
#  pull_request:
#    branches: [main]
#    types: [closed]
#  schedule: 
#    - cron: '37 0 * * *'
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#      - uses: actions/setup-java@v1
#        with:
#          java-version: '17.x'
#      - uses: subosito/flutter-action@v2
#        with:
#          flutter-version: '3.24.1'
#          channel: 'stable'
#      - name: Create key.properties file
#        run: |
#          echo "storePassword=.atb@_"> android/key.properties
#          echo "keyPassword=.atb@_">> android/key.properties
#          echo "keyAlias=upload">> android/key.properties
#          echo "storeFile=../../upload-keystore.jks">> android/key.properties
#
#      - run: flutter pub get
#      - run: flutter build apk --split-per-abi --flavor dev -t lib/main_dev.dart
#      - uses: actions/upload-artifact@v4
#        with:
#          name: release-apk
#          path: build/app/outputs/flutter-apk/app-arm64-v8a-dev-release.apk
#
#      - name: Upload to gdrive
#        uses: adityak74/google-drive-upload-git-action@main
#        with:
#          credentials: ${{ secrets.GOOGLE_DRIVE_KEY }}
#          filename: "build/app/outputs/flutter-apk/app-arm64-v8a-dev-release.apk"
#          folderId: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
#          overwrite: "true"
#
#      - uses: actions/checkout@v2